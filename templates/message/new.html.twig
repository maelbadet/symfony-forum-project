{# templates/message/new.html.twig #}
<link rel="stylesheet" href="{{ asset('css/form.css') }}">

    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button class="sign" type="submit">Envoyer</button>
    {{ form_end(form) }}